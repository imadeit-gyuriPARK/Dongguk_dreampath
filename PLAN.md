# 과제 진행 메모

## 목표
첨부 데이터셋(댓글 1,000건)에서 학교명을 추출해 학교별 댓글 수를 집계.

## 검토자
Microsoft · Cloud & AI Solution 엔지니어 현직자가 리뷰 예정.
→ 코드뿐 아니라 README, 처리 로직의 설명력이 중요.

## 접근 순서
1. **데이터 탐색(EDA)** — 컬럼 구조, 인코딩, 결측치, 댓글 샘플 확인.
   학교명이 텍스트 안에 자연어로 섞여 있는지, 별도 컬럼인지부터 판단.
2. **추출 방법 결정** — 학교명 사전 매칭 vs 정규식 vs NER.
   데이터 성격상 사전 기반 매칭이 현실적이고 설명하기도 쉬움.
3. **정규화 규칙 설계** — "동국대"/"동국대학교"/"DGU" 같은 이표기, 오탈자,
   축약형을 표준명으로 매핑하는 규칙 정의. (설명력이 드러나는 핵심 부분)
4. **엣지케이스 처리 방침 결정** — 학교명 없는 댓글, 복수 학교 언급 댓글,
   오검출 케이스를 어떻게 카운트할지 미리 규칙화.
5. **집계 & 검증** — 학교별 카운트 집계 후 합계가 1,000건과 맞는지,
   상위 학교들이 상식적으로 말이 되는지 스팟체크.
6. **시각화 요약** — 상위 N개 학교 막대그래프.
7. **README 작성** — 문제 정의 → 접근 방법과 이유 → 정규화/엣지케이스 처리
   기준 → 실행 방법 → 결과 요약 → 한계점/개선 여지.

## 프로젝트 구조
```
dreampath/
├── data/
│   ├── raw/          # 원본 댓글/사전 데이터셋
│   ├── interim/      # 파이프라인 중간 산출물
│   └── processed/    # 학습/분석에 바로 쓰는 최종본 (GT 사전 등)
├── notebooks/        # EDA, 파이프라인 단계별 노트북 ({단계번호}-{설명}.ipynb)
├── src/
│   └── data/         # 재사용 전처리 코드 (preprocessing.py)
├── results/          # 실험 결과 (날짜별 폴더)
├── reports/
│   └── figures/      # 시각화 결과
├── .gitignore
├── requirements.txt
└── README.md         # 분석 끝나고 결과 보면서 작성 예정
```

## 현재 상태
- [x] GitHub 저장소(`imadeit-gyuriPARK/Dongguk_dreampath`) 로컬에 clone, origin 연결 완료
- [x] 표준 폴더 구조 생성
- [ ] 원본 데이터셋을 `data/raw/`에 넣기
- [ ] EDA 시작


### 계획
1. 초,중,고,대,학교 이용해서 일단 뽑아오기 school_name이라는 열 뽑아오고 
2. 우리가 가지고 있는 초, 중, 고,대 이름과 맞지 않는 경우는 애초에 잘못 만든 경우인거니까 여기서 한번 또 걸러서 보고 